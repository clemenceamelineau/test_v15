<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="transfer_view_order_form" model="ir.ui.view">
        <field name="name">transfer.view.order.form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_draft']" position="after">
                <button name="btn_transfer" string="Transfert" class="btn btn-primary" type="object" confirm="Are you sure you want to continue ?"/>
            </xpath>
           
        </field>
    </record>
</odoo>